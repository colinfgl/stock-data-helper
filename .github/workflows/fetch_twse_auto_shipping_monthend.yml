name: Fetch TWSE auto shipping month-end
on:
  push:
    paths:
      - 'scripts/fetch_twse_auto_shipping_monthend.py'
      - '.github/workflows/fetch_twse_auto_shipping_monthend.yml'
  workflow_dispatch:
permissions:
  contents: read
jobs:
  collect:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python -m pip install --disable-pip-version-check requests
      - name: Collect month-end industry data
        shell: bash
        run: |
          set +e
          python scripts/fetch_twse_auto_shipping_monthend.py
          code=$?
          echo "$code" > output_industry_monthend/exit_code.txt
          exit 0
      - uses: actions/upload-artifact@v4
        with:
          name: p1b-industry-auto-shipping-monthend-20260828
          path: output_industry_monthend/
          if-no-files-found: error
      - name: Enforce gate
        run: test "$(cat output_industry_monthend/exit_code.txt)" = "0"
